{
	"$schema": "https://json.schemastore.org/package.json",
	"name": "conclaude",
	"version": "0.0.3",
	"description": "Claude Code hook handler CLI tool that processes hook events and manages lifecycle hooks for tool usage, session management, and transcript processing",
	"keywords": [
		"claude",
		"claude-code",
		"hooks",
		"cli",
		"typescript",
		"bun",
		"ai",
		"automation"
	],
	"author": "Conner Ohnesorge",
	"license": "MIT",
	"type": "module",
	"main": "dist/index.js",
	"bin": {
		"conclaude": "dist/conclaude"
	},
	"files": [
		"dist/**/*",
		"README.md"
	],
	"scripts": {
		"build": "bun build src/index.ts --target=node --outfile=dist/conclaude --external=c12 --external=winston --external=yargs && chmod +x dist/conclaude",
		"build:js": "bun build src/index.ts --target=node --outfile=dist/index.js --external=c12 --external=winston --external=yargs",
		"prepublishOnly": "bun run build:js && bun run build",
		"test": "bun test",
		"lint": "bun x tsc --noEmit",
		"typecheck": "bun x tsc --noEmit",
		"postinstall": "command -v bun2nix >/dev/null 2>&1 && bun2nix -o bun.nix || echo 'bun2nix not available, skipping bun.nix generation'"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/minimatch": "^6.0.0",
		"@types/yargs": "^17.0.33"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"c12": "^3.2.0",
		"minimatch": "^10.0.3",
		"winston": "^3.17.0",
		"yargs": "^18.0.0"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/connerohnesorge/conclaude.git"
	}
}
